{
  "functions": [
    {
      "source": "yuppeeai-backend",
      "codebase": "backend",
      "runtime": "nodejs24",
      "env": ["OPENAI_MODEL=gpt-4o-mini"],
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "npm --prefix .. run build:contracts",
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ]
    }
  ]
}
